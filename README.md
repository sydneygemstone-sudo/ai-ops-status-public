# AI Operations Public Status

Public, redacted CEO-level status for lightweight web and voice lookup.

The private interaction repository is the source of truth. This repository contains only a one-way, allowlisted projection. It never contains complete briefs, personal names, amounts, local paths, repository names, worker identifiers, raw logs, prompts, hashes, credentials, or private source material.

Read [STATUS.md](STATUS.md) for the latest human-readable status or [current.json](current.json) for the machine-readable projection.
